<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fbpro.dao.ScheduleDAO">

	<!-- 전체 스케쥴 조회 -->
	<select id="getAllSchedules" resultType="fbpro.vo.ScheduleVO">

		SELECT * FROM schedules

	</select>

	<!-- 특정 스케쥴 조회 -->
	<select id="getScheduleByUserId"
		resultType="fbpro.vo.ScheduleVO">
		SELECT * FROM schedules WHERE user_id = #{userId}

	</select>

	<!-- 스케쥴 추가 -->
	<insert id="insertSchedule">
		INSERT INTO schedules (schedule_code, user_id, shift_code, start_time,
		end_time, created_at)
		VALUES (#{scheduleCode}, #{userId}, #{shiftCode}, #{startTime}, #{endTime},
		#{createdAt})
	</insert>

	<!-- 스케쥴 업데이트 -->
	<update id="updateSchedule">
		UPDATE schedules
		SET user_id =#{userId},
		shift_code = #{shiftCode},
		start_time =
		#{startTime},
		end_time = #{endTime}
		WHERE schedule_code = #{scheduleCode}

	</update>

	<delete id="deleteSchedule">
		DELETE FROM schedules WHERE schedule_code = #{scheduleCode}
	</delete>

</mapper>